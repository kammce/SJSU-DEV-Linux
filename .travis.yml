matrix:
    include:
        - os: linux
          sudo: true
          dist: trusty
          group: edge
          install:
            - sudo apt install python-pip
            - pip install virtualenv
            - ./setup
        - os: linux
          sudo: true
          dist: trusty
          group: edge
          install:
            - sudo apt install python-pip
            - pip install virtualenv
            - ./setup-py3
        - os: osx
          before_install:
            - pip install virtualenv
            - ./setup
script:
  - cd ./firmware/FreeRTOS/
  - make build
  - ls ./bin/HelloWorld.elf
  - ls ./bin/HelloWorld.hex
  - ls ./bin/HelloWorld.lst
  - ls ./bin/HelloWorld.map